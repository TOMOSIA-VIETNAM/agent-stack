node_modules/
*.log
.DS_Store

# Scratch target for `npm run try` — generated output, never committed.
.agent-stack-try/

# Local secrets.
.env
.env.local

# Skills the harness installs into this repo's own .claude/ — third-party content,
# which agent-stack may only carry through upstream.yaml with a pinned commit.
.claude/
